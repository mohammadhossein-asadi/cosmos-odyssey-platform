import type { Metadata } from "next";
export const metadata: Metadata = { title: "Black Holes | Cosmos Odyssey", description: "Experience the most extreme objects in the universe." };
export default function BlackHoleLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
